#mybatis
spring:
  datasource:
    host: ${config.getHost()}
    port: ${config.getPort()}
    database: ${config.getDatabase()}
    username: ${config.getUsername()}
    password: ${config.getPassword()}

