# 生成契约

- Maven 参数优先于配置文件；非交互缺少必填项时失败。
- 表名去重并从数据库读取元数据；数据库账号仅需元数据读取权限。
- `overwrite=false` 时保留已有非空文件，`skipIfExists` 模板始终保护手工扩展。
- 基础包、项目名、表/实体名和模板输出必须规范化并限制在目标根。
- 类型、nullable、长度、主键、`delete_flag` 和租户列决定生成模型与校验。
- 生成后检查未解析模板、错误包路径、Secret 和意外覆盖，并构建目标项目。
